---
title: Credit Card
description: An input field for card number supported for Amex, JCB, Mastercard, and Visa.
navigation: [{
  name: "Installation",
  link: "#installation"
}, {
  name: "Usage",
  link: "#usage"
}, {
  name: "API Reference",
  link: "#api-reference",
  child: [{
    name: "CreditCard",
    link: "#creditcard"
  }]
}]
---

<PreviewCreditCard />

## Installation
Install the component from your command line.

<NpmInstall packageName="@zenbu-ui/input" />

## Usage

```jsx
import { Input } from '@zenbu-ui/input'

const App = () => {
  return(
    <Input.CreditCard name="tester" placeholder="Card Number"
    onChange={(cardNumber, valid, type) => {
      ...
    }} />
  )
}

export default App
```

## API Reference
Below shows only the specific properties present in this component. Design customization such as coloring, rounded, and others can be seen in the [styling section](/docs/styling).

### CreditCard
<CreditCardProps />
